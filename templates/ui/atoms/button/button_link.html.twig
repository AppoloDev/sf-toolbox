{% extends '@SFToolbox/ui/atoms/button/_button.html.twig' %}

{% block button_content %}
    {% if allowDisplay is same as(true) %}
        <a
                href="{{ link }}"
                target="{{ target }}"
                class="btn btn-size-{{ size }} btn-color-{{ color }} btn-mode-{{ mode }} {{ tooltip ? 'hs-tooltip-toggle' }} {{ block ? 'w-full' }}"
        >
            {{ parent() }}
        </a>
    {% endif %}
{% endblock %}
