<dismiss-toast id="dismiss-toast">
    <div class="toast toast-{{ color }}" role="alert">
        <div class="content">
            {{ message }}
            <div data-hs-remove-element="#dismiss-toast">
                <twig:Button
                        color="{{ color }}"
                        mode="ghost"
                        icon="{{ ux_icon('ph:x') }}"
                        size="small"
                    />
            </div>
        </div>
    </div>
</dismiss-toast>

