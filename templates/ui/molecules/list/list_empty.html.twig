<div class="max-w-sm w-full flex flex-col justify-center mx-auto space-y-5 p-8">
    <div class="flex flex-col items-center gap-2">
        <div class="mb-4">
            {% block ListEmptyIllustration %}
                {{ source('@SFToolbox/illustrations/list_empty.svg') }}
            {% endblock %}
        </div>

        {% if title is not null %}
            <div class="font-semibold text-gray-800 text-center">
                {{ title }}
            </div>
        {% endif %}

        {% if description is not null %}
            <p class="text-sm text-gray-600 text-center">
                {{ description }}
            </p>
        {% endif %}
    </div>

    {% if block('ListEmptyButton') is defined %}
        <div class="flex justify-center mt-5">
            {{ block('ListEmptyButton') }}
        </div>
    {% endif %}
</div>
