{% block geo_localizable_widget %}
   <geo-complete api-key="{{ googleMapApiKey }}" source=".source" target="#{{ id }}" autocompleteOptions="{{ autocompleteOptions|json_encode }}">
      {{ form_row(form) }}
      <input type="text" placeholder="{{ placeholder|trans }}" class="source">
      <ul>
      </ul>
   </geo-complete>
{% endblock %}
