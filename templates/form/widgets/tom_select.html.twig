{% block tom_select_widget %}
    <tom-select options="{{ configuration|json_encode }}" addText="{{ 'add'|trans }}" noMatchText="{{ 'no_match'|trans }}">
        {{ form_widget(form) }}
    </tom-select>
{% endblock %}
